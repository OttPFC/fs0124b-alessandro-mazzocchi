import { ColoriDirective } from './colori.directive';

describe('ColoriDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoriDirective();
    expect(directive).toBeTruthy();
  });
});
